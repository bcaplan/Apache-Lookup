# -*- ruby -*-

require 'rubygems'
require 'hoe'
require './lib/apache_lookup.rb'

Hoe.new('apache_lookup', ApacheLookup::VERSION) do |p|
  # p.rubyforge_name = 'apache_lookupx' # if different than lowercase project name
  # p.developer('FIX', 'FIX@example.com')
end

# vim: syntax=Ruby
